# archive/

Superseded scaffolding kept for reference only — **not part of the Apex Abstracts tools**
and not exercised by `npm test`.

- `portal-generator/` — the earlier "reusable customer portal generator" project
  (Next.js/SQLite/Stripe scaffolding: `src/`, `db/`, `templates/`, `output/`, `config/`,
  its `package.json`/`package-lock.json`, and its README). Some of its integration adapters
  (`src/lib/integrations/*`) and `db/schema.sql` are referenced in `docs/ROADMAP.md` as
  possible bones for a future Phase-2 backend.
