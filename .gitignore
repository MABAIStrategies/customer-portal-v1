# Address-trial inputs/outputs from third-party consumer property reports contain
# residents' personal data (names, ages, deceased flags) and agent contact info.
# Kept local on purpose — never commit.
demo/address-trials/

# Claude Code worktrees / agent state — never commit
.claude/
